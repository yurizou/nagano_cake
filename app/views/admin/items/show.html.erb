<h1 class="border text-dark">商品詳細</h1>
<div class="container">
<div class="row">
  <div class="col-4 md-5 float-left">
  <p>商品画像</p><%= image_tag @item.image,size:"300x200" %>
  </div>
  <div class="col-8">
    <p>商品名</p><%= @item.name %>
    <p>商品説明</p><%= @item.introduction %>
    <p>ジャンル </p><%= @genre.name %>
    <p>税込価格（税抜価格)</p><%= @item.price %>
    <p>販売ステータス</p>
    <p><% if @item.is_active %></p>
    <p class="text-success">販売中</p>
    <% else %>
    <p class="text-secondary">販売停止中</p>
    <% end %>
    <%= link_to "編集する", edit_admin_item_path, class: "btn btn-success" %>
  </div>
 </div>
</div>